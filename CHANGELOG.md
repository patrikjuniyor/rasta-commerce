# تغییرات راستا کامرس

همه تغییرات مهم این پروژه در این فایل ثبت می‌شوند. قالب از [Semantic Versioning](https://semver.org/lang/fa/) پیروی می‌کند.

## [2.7.1] — ۲۰۲۶-۰۸-۱۸

### رفع باگ‌های بحرانی

- **رفع خطای fatal بدون WooCommerce:** فراخوانی `rasta_render_sticky_add_to_cart()` در `footer.php` بدون guard بود؛ چون این تابع فقط در لایهٔ سازگاری WooCommerce (که شرطی بارگذاری می‌شود) تعریف شده، در سایت‌های فاقد WooCommerce با خطای «undefined function» صفحه را از کار می‌انداخت. اکنون با `function_exists` محافظت شده است.
- **رفع تداخل نام تابع:** `rasta_product_is_new()` هم در `products.php` (فروشگاه داخلی) و هم در `woocommerce.php` تعریف شده بود؛ با فعال‌شدن WooCommerce خطای «cannot redeclare» رخ می‌داد. نسخهٔ WooCommerce به `rasta_wc_product_is_new()` تغییر نام یافت.

## [2.7.0] — ۲۰۲۶-۰۸-۱۵

### افزوده شد

- **راستا ساز (صفحه‌ساز درگ‌اند‌دراپ):** ماژول صفحه‌ساز واقعی داخل قالب با متاباکس ادیتور در پیشخوان، ذخیرهٔ چیدمان به‌صورت JSON در post meta و رندر سمت سرور در فرانت‌اند.
- **۱۱ المنت پایه:** تیتر، متن، دکمه، تصویر، جداکننده، فاصله، فراخوان اقدام (CTA)، لیست ویژگی، شبکه محصول، دسته‌بندی و نظرات مشتریان.
- **شورت‌کد `[rasta_builder id="…"]`** برای نمایش چیدمان ذخیره‌شدهٔ هر صفحه در هر جای قالب.
- **تست قرارداد جدید** برای رجیستری المنت‌ها، sanitize چیدمان، رندر و ثبت hookها به CI اضافه شد.

## [2.6.0] — ۲۰۲۶-۰۸-۱۵

### افزوده شد

- **صفحه خوش‌آمدگویی و نصب:** پس از فعال‌سازی قالب، کاربر یک‌بار به صفحه «خوش آمدید» (نمایش ← خوش آمدید) هدایت می‌شود؛ شامل راهنمای شروع سریع چهار مرحله‌ای، کارت‌های دسترسی سریع (سفارشی‌سازی، داشبورد فروشگاه، افزودن محصول، تنظیمات) و نسخهٔ قالب.
- **تست قرارداد جدید** برای صفحهٔ خوش‌آمدگویی (ثبت منو، سطح دسترسی و اولویت ریدایرکت) به CI اضافه شد.

## [2.5.0] — ۲۰۲۶-۰۸-۱۵

### رفع باگ‌های بحرانی

- **رفع شروع نشست سبد خرید:** شرط درهم‌ریختهٔ `rasta_start_session()` باعث می‌شد نشست PHP هرگز آغاز نشود و سبد خرید داخلی کاملاً از کار بیفتد. نشست اکنون به‌درستی هم در فرانت‌اند و هم در درخواست‌های AJAX آغاز می‌شود.
- **رفع nonce تسویه‌حساب:** جاوااسکریپت nonce جستجو را ارسال می‌کرد در حالی که سرور nonce تسویه را بررسی می‌کرد؛ ثبت سفارش همیشه شکست می‌خورد. یک nonce اختصاصی (`checkoutNonce`) اضافه شد.
- **رفع خطای ۴۰۴ پس از خرید:** ریدایرکت به صفحه‌ای ناموجود می‌رفت. صفحه و شورت‌کد «تشکر از خرید» با خلاصهٔ سفارش اضافه شد.

### رفع باگ‌های UX

- **به‌روزرسانی زندهٔ مینی‌سبد:** پس از افزودن/حذف/تغییر تعداد با AJAX، محتوای مینی‌سبد (اقلام، جمع کل و نوار ارسال رایگان) اکنون بدون رفرش بازسازی می‌شود.
- **نمایش صحیح قیمت فروش ویژه:** کارت محصول و صفحهٔ محصولِ فروشگاه داخلی اکنون قیمت اصلی (خط‌خورده) و قیمت فروش را به‌درستی جدا نشان می‌دهند.

### افزودنی‌ها و استانداردها

- **کنترل واحد پول** (تومان/ریال) در پوسته‌ساز — پیش‌تر در کد استفاده می‌شد اما قابل تنظیم نبود.
- **صفحهٔ جستجو (`searchform.php`)** با طراحی RTL و هماهنگ با قالب.
- **تعریف `$content_width`** برای ابعاد صحیح embed.
- **گارد امنیتی `ABSPATH`** به ۱۲ فایل قالب فاقد آن اضافه شد.
- ستون‌های شبکهٔ محصولات ووکامرس اکنون از تنظیم پوسته‌ساز پیروی می‌کنند.
- اصلاح فراخوانی تکراری دسته‌بندی‌ها، لینک ادمین هاردکد و تابع منسوخ `current_time('timestamp')`.
- سازوکار ساخت خودکار صفحات فروشگاه هنگام تغییر نسخهٔ قالب (برای دریافت صفحهٔ جدید پس از آپدیت).

## [2.4.0] — ۲۰۲۶-۰۸-۱۳

### بهبود یافت

- **پوسته‌ساز:** نمایش شرطی کنترل‌ها (`active_callback`) — تنظیمات مرتبط با حالت تاریک، واتساپ و حالت تعمیر فقط وقتی فعال باشند نمایش داده می‌شوند.
- **پوسته‌ساز:** پیش‌نمایش زنده (selective refresh) برای متن نوار اطلاع‌رسانی و محتوای هیرو؛ بدون بازبارگذاری کامل صفحه.
- **پوسته‌ساز:** کنترل «چیدمان شبکه محصولات» از عدد به گزینه‌های انتخابی برچسب‌دار (۲ تا ۵ ستون) تبدیل شد.
- **داشبورد فروشگاه:** کارت‌های آماری با آیکون و رنگ اختصاصی، نوار دسترسی سریع (افزودن محصول، سفارش‌ها، تنظیمات)، نرخ تکمیل سفارش و حالت‌های خالی زیباتر.
- **صفحه تنظیمات فروشگاه:** چیدمان کارتی با بخش‌بندی واضح و کلید اطلاع‌رسانی برجسته‌تر.

## [2.3.0] — ۲۰۲۶-۰۸-۱۳

### افزوده شد

- **دکمه شناور واتساپ:** دکمه چت واتساپ در تمام صفحات با شماره، پیام پیش‌فرض و کلید فعال‌سازی قابل تنظیم از پوسته‌ساز (بخش «پشتیبانی واتساپ»).
- **ایمیل اطلاع‌رسانی سفارش:** ارسال خودکار ایمیل HTML فارسی به مدیر فروشگاه هنگام ثبت هر سفارش جدید (شامل مشخصات مشتری و اقلام سفارش)، با قابلیت خاموش/روشن.
- **صفحه تنظیمات فروشگاه:** صفحه جدید در «فروشگاه ← تنظیمات فروشگاه» برای مدیریت ایمیل مدیریت، موضوع ایمیل و کلید اطلاع‌رسانی سفارش (Settings API).
- **تست‌های قرارداد جدید:** تست واحد برای صفحه تنظیمات فروشگاه و ایمیل اطلاع‌رسانی سفارش به CI اضافه شد.

## [2.2.0] — ۲۰۲۶-۰۸-۱۳

### افزوده شد

- **حالت تعمیر (Coming Soon):** صفحه «فروشگاه به‌زودی بازمی‌گردد» برای بازدیدکنندگان واردنشده، با تیتر و متن قابل ویرایش از پوسته‌ساز (بخش «وضعیت فروشگاه»). مدیران و کاربران واردشده سایت را عادی می‌بینند.
- **نشانگر حالت تعمیر:** هشدار در نوار مدیریت و بنر اطلاع‌رسانی در «نمای کلی فروشگاه» وقتی حالت تعمیر فعال است.
- **تست‌های قرارداد جدید:** تست واحد اختصاصی برای داشبورد مدیریت، پوسته‌ساز و حالت تعمیر (سه فایل PHP در `tests/php`) به CI اضافه شد.

## [2.1.0] — ۲۰۲۶-۰۸-۱۳

### افزوده شد

- **حالت تاریک** در پوسته‌ساز: دکمه تغییر روشن/تاریک در هدر، تشخیص تنظیم سیستم کاربر، گزینه «حالت تاریک پیش‌فرض» و پالت رنگ تاریک با CSS variables (بدون FOUC با اسکریپت بوت).
- **سفارشی‌سازی پایین صفحه:** متن معرفی فروشگاه و متن کپی‌رایت (با جای‌نگهدارهای `{year}` و `{site}`) قابل ویرایش از پوسته‌ساز.
- **شبکه‌های اجتماعی بیشتر:** واتساپ، توییتر/ایکس و آپارات به فهرست پیوندهای پایین صفحه افزوده شد.
- **نوار اطلاع‌رسانی بهبودیافته:** پیوند و متن دلخواه + دکمه بستن (با ذخیره در localStorage) قابل فعال‌سازی از پوسته‌ساز.
- **تعداد ستون محصولات:** کنترل تعداد ستون شبکه محصولات (۲ تا ۵) در پوسته‌ساز.
- **داشبورد نمای کلی فروشگاه** در «فروشگاه ← نمای کلی فروشگاه» با آمار زنده محصولات، سفارش‌ها، درآمد، هشدار موجودی کم، آخرین سفارش‌ها و محصولات کم‌موجود.
- **ستون‌های سفارشی فهرست محصولات:** نمایش کد محصول (SKU)، قیمت (با نمایش تخفیف) و وضعیت موجودی، به‌همراه فیلتر سریع «موجودی کم» و «ناموجود».

### بهبود یافت

- همگام‌سازی شماره نسخه در تمام فایل‌ها (style.css، functions.php، package.json، اسکریپت‌های بسته‌بندی، POT و تست‌ها) روی 2.1.0.
- رفع خطاهای پیشین stylelint (range notation، color function و vendor prefix) و جایگزینی `innerHTML` با `textContent` در به‌روزرسانی جمع سبد خرید برای امنیت بیشتر.
- فایل POT با رشته‌های قابل ترجمه جدید بازتولید شد.

## [2.0.0] — ۲۰۲۶-۰۸-۱۱

### افزوده شد

- فروشگاه مستقل داخلی (نوع نوشته محصول، سفارش، سبد خرید سمت سرور و پرداخت) بدون وابستگی به WooCommerce؛ WooCommerce به‌صورت اختیاری تجربه را ارتقا می‌دهد.

## [1.4.1] — ۲۰۲۶-۰۸-۰۸

### افزوده شد

- پنل فارسی «هسته راستا» در نمایش ← هسته راستا برای فعال/غیرفعال‌سازی مستقل هر یک از ۱۰ ویجت Elementor.
- سیاست شفاف الهام‌گیری از آرشیوهای مرجع، بدون کپی یا بازتوزیع کد و دارایی‌های دارای مجوز.

## [1.4.0] — ۲۰۲۶-۰۸-۰۴

### افزوده شد

- افزونه `Rasta Commerce Core for Elementor` با ۱۰ ویجت فروشگاهی RTL-native: Hero، تیتر بخش، شبکه/ریل محصول، دسته‌بندی، بنر، اعتماد، مجله، کارت ویژگی و FAQ.
- فایل CSS اختصاصی Elementor، افزونه ZIP مستقل، فایل POT و تست قرارداد ثبت هر ۱۰ ویجت.
- ادغام افزونه Core در پکیج آماده‌سازی راست چین و CI.

## [1.3.0] — ۲۰۲۶-۰۸-۰۴

### افزوده شد

- فونت Vazirmatn با مجوز OFL و بارگذاری محلی در frontend و editor.
- لایه جلالی برای تاریخ نوشته‌ها و دیدگاه‌ها، به‌همراه تست قرارداد تاریخ.
- child theme رسمی، فایل‌های POT قالب و افزونه زرین‌پال، و به‌روزرسانی متن‌های قابل ترجمه.
- راهنمای PDF فارسی، WXR محتوای آغازین، اسکریپت child theme و preflight/pipeline آماده‌سازی پکیج راست چین.
- چک‌لیست عرضه، راهنمای مالکیت دارایی و گزارش آمادگی راست چین.

### بهبود یافت

- افزونه زرین‌پال 1.0.1 با بارگذاری فایل ترجمه و ثبت callback خارج از gateway lazily-loaded برای اطمینان از اجرای WC-API.

## [1.2.0] — ۲۰۲۶-۰۸-۰۴

### افزوده شد

- افزونه مستقل `Rasta ZarinPal Gateway for WooCommerce` با API v4 زرین‌پال، محیط Sandbox، تنظیم Merchant ID، تبدیل ریال/تومان و گزارش فنی اختیاری.
- درخواست پرداخت با callback دارای order key، تطبیق Authority ذخیره‌شده و verify سمت سرور پیش از کامل‌شدن سفارش.
- ثبت RefID در سفارش و صفحه تشکر، بدون نگه‌داری شماره کارت یا `card_hash`.
- ساخت ZIP مستقل برای افزونه درگاه و بررسی ایستای مسیرهای API/امنیت callback در تست‌ها.

## [1.1.0] — ۲۰۲۶-۰۸-۰۴

### افزوده شد

- نمایش سریع محصول با AJAX و ساخت امن DOM؛ محصول ساده از همان drawer قابل افزودن است و variationها کاربر را به صفحه کامل هدایت می‌کنند.
- drawer علاقه‌مندی و شمارنده هدر با نگه‌داری محلی و خصوصی در مرورگر.
- انتخاب و مقایسه حداکثر چهار محصول با tray ثابت و جدول قیمت، موجودی، SKU و ویژگی‌های WooCommerce.
- ریل محصولات اخیراً دیده‌شده بدون tracker خارجی.
- نوار چسبان خرید در صفحه محصول، شمارش‌گر تخفیف زمان‌دار و نشان‌های «تازه»/موجودی پایین.
- نوار پیشرفت اختیاری ارسال رایگان در mini-cart با آستانه قابل تنظیم.
- کنترل‌های جدید Customizer برای فعال/غیرفعال‌سازی ابزارها و تنظیم آستانه‌ها.
- سازگاری ظاهری سبک با Variation Swatches، YITH Wishlist/Compare و فیلترهای رایج WooCommerce.
- تست‌های JSDOM برای مقایسه و نمایش سریع و سند بررسی قابلیت‌های افزونه‌ای.

## [1.0.1] — ۲۰۲۶-۰۸-۰۴

### بهبود یافت

- پیش‌نمایش رسمی قالب با مرورگر واقعی در ابعاد دسکتاپ و موبایل بازتولید شد تا متن فارسی و چیدمان RTL در تصویر معرفی هم دقیق باشد.

## [1.0.0] — ۲۰۲۶-۰۸-۰۴

### افزوده شد

- قالب فروشگاهی راست‌چین و فارسی‌محور با پشتیبانی بومی WooCommerce.
- صفحه نخست حرفه‌ای با هیرو، دسته‌بندی‌ها، ریل محصولات پویا، نوار اعتماد و مجله.
- کارت محصول اختصاصی با افزودن AJAX به سبد، علاقه‌مندی محلی و وضعیت موجودی/تخفیف.
- جست‌وجوی سریع محصولات با nonce، پاک‌سازی ورودی و ساخت امن DOM.
- دراور جست‌وجو و سبد خرید با کنترل کیبورد، `Escape` و focus trap.
- طراحی واکنش‌گرا، فایل `rtl.css` و استفاده گسترده از CSS Logical Properties.
- Customizer کم‌حجم برای رنگ‌ها و محتوای هیرو.
- CI گیت‌هاب برای lint، تست، PHP syntax check و ساخت ZIP قابل نصب.
