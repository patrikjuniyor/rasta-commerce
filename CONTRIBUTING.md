# مشارکت در راستا کامرس

از مشارکت شما استقبال می‌شود. قبل از ارسال Pull Request لطفاً این اصول را رعایت کنید:

1. شاخه‌ای با نام روشن بسازید؛ نمونه: `fix/mobile-cart`.
2. برای قابلیت‌های جدید ابتدا Issue باز کنید تا دامنه تغییر روشن باشد.
3. برای متن‌های قابل‌نمایش از توابع ترجمه وردپرس و text domain `rasta-commerce` استفاده کنید.
4. خروجی پویا را بر اساس context با `esc_html()`, `esc_attr()`, `esc_url()` یا `wp_kses_post()` escape کنید.
5. برای داده‌ی ورودی از `sanitize_*()` و برای درخواست‌های AJAX از nonce استفاده کنید.
6. از کپی‌کردن templateهای کامل WooCommerce اجتناب کنید؛ ابتدا hooks را ترجیح دهید تا به‌روزرسانی‌های ووکامرس کم‌خطر بمانند.
7. پیش از PR این فرمان را اجرا کنید:

```bash
npm ci
npm run check
npm run package
```

## استاندارد طراحی

- چیدمان باید با `dir="rtl"` طبیعی بماند؛ تا حد ممکن از `margin-inline`، `padding-inline` و `inset-inline` استفاده کنید.
- برای حالت focus، کیبورد و عرض‌های ۹۰۰ و ۶۴۰ پیکسل تست دستی انجام دهید.
- تصویر، فونت یا کد دارای مجوز نامشخص وارد مخزن نکنید.
