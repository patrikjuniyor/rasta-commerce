# ممیزی کیفیت — راستا کامرس

فهرست نقص‌های شناسایی‌شده و وضعیت رفع آن‌ها.

## بحرانی (قالب را از کار می‌اندازد)

- [x] `rasta_start_session()` — شرط درهم و معکوس؛ نشست هرگز شروع نمی‌شد و سبد خرید کاملاً خراب بود.
- [x] عدم تطابق nonce تسویه‌حساب — جاوااسکریپت nonce جستجو را می‌فرستاد ولی سرور nonce تسویه را چک می‌کرد؛ ثبت سفارش همیشه شکست می‌خورد.
- [x] ریدایرکت تسویه به `/order-received/` بدون قالب/صفحه → خطای ۴۰۴ پس از خرید.

## مهم

- [x] `$content_width` تعریف نشده بود (الزام Theme Check برای ابعاد embed).
- [x] `rasta_currency` در چند فایل استفاده شده ولی هیچ کنترلی در پوسته‌ساز برایش ثبت نشده بود.
- [x] `rasta_loop_shop_columns` و `rasta_related_products_args` مقدار ثابت ۴ برمی‌گرداندند و تنظیم ستون محصولات را نادیده می‌گرفتند.
- [x] `searchform.php` وجود نداشت؛ فرم جستجوی پیش‌فرض وردپرس (LTR و بدون استایل) نمایش داده می‌شد.

## استانداردها (Theme Check / امنیت)

- [x] گارد `ABSPATH` در ۱۲ فایل قالب (single, archive, index, search, page, 404, sidebar, comments و template-parts) نبود.
- [x] `single.php` تابع `get_the_category_list` را دو بار صدا می‌زد.
- [x] لینک ادمین با `home_url('/wp-admin/')` هاردکد شده و برای بازدیدکننده معمولی نمایش داده می‌شد.
- [x] `rasta_get_the_jalali_date` مقدار `false` را مدیریت نمی‌کرد.
- [x] `current_time('timestamp')` منسوخ (deprecated).

## افزودنی‌های ضروری فروشگاه

- [x] صفحه و شورت‌کد «تشکر از خرید» (order-received) با خلاصه سفارش.
- [x] کنترل انتخاب واحد پول (تومان/ریال) در پوسته‌ساز.
