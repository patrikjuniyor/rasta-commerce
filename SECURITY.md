# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

در حال حاضر فقط شاخه‌ی `main` و نسخه‌ی پایدار اخیر پشتیبانی می‌شوند.

## گزارش آسیب‌پذیری

لطفاً آسیب‌پذیری‌های احتمالی را به‌صورت عمومی در Issue منتشر نکنید. یک Issue با عنوان `Security contact requested` باز کنید و در آن جزئیات بهره‌برداری را وارد نکنید؛ برای ادامه‌ی ارتباط یک کانال خصوصی ترتیب داده می‌شود.

## اصول امنیتی قالب

- endpoint جست‌وجوی عمومی با nonce محافظت شده و ورودی را sanitize می‌کند.
- نتایج جست‌وجو در مرورگر با `textContent` ساخته می‌شوند، نه HTML خام.
- هیچ tracker، فونت CDN یا کلید محرمانه‌ای در قالب وجود ندارد.
- پرداخت و مدیریت سفارش کاملاً به WooCommerce و افزونه‌های درگاه معتبر واگذار می‌شود.
